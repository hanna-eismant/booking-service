#parse('layouts/header.vm')

<h3>Event information
  <small>$event.name</small>
</h3>

<a class="btn btn-primary btn-s" style="margin-bottom: 15px;" role="button" href="/event/$event.name/assign">
  Assign auditorium
</a>

#if($event.eventInstances.size() != 0)

<table class="table table-striped">
  <thead>
  <tr class="info">
    <td><strong>Date</strong></td>
    <td><strong>Auditorium</strong></td>
    <td><strong>Free tickets</strong></td>
    <td></td>
  </tr>
  </thead>
  <tbody>
      #foreach( $ins in $event.eventInstances )
      <tr>
        <td>$ins.date.toString("MM/dd/yyyy HH:mm")</td>
        <td>$ins.auditorium.name</td>
        <td>
            $ins.freeTicketCount form $ins.auditorium.seats
          (<a href="/events/tickets">more information</a>)
        </td>
        <td>
            #if ($ins.freeTicketCount != 0)
              <a class="btn btn-primary btn-xs" role="button" href="/tickets/book/$ins.id">
                Book ticket
              </a>
            #end
        </td>
      </tr>
      #end
  </tbody>
</table>

#end

#parse('layouts/footer.vm')
